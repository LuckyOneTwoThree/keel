<aside>  
🐾

**一句话定位**：把原来的“单工作区=单项目”升级为**三段式结构**——共享层(全局唯一) + 模板层(骨架母版) + 项目层(逐项目独立实例)；并在单项目内部补上一条 **REQ- 需求追溯主线**，把需求→决策→方案→原型→验收全链路串起来。项目数据物理隔离、零耦合；通用规则与知识全局沉淀、不漂移。本方案只定**结构与协议**，不含具体项目内容，确认后按迁移步骤落盘。

</aside>

## 一、设计目标

1. **项目解耦**：不同项目的进度/决策/风险/沟通互不污染(物理隔离)。
2. **知识可沉淀**：通用经验跨项目复用，项目上下文本地保留(双层模型)。
3. **全链路可追溯**：以 REQ- 为脊椎，任一需求可回溯其决策、方案、原型、风险、验收。
4. **规则不漂移**：宪章与 AI 契约全局唯一一份。
5. **可扩展 + 可检索**：编号统一防冲突，增设派生索引层。
6. **守住原设计灵魂**：单一真相源、只追加不回改、AI 最小权限、简单优先。

<aside>  
🔒

**三条地基约束(重构中不可动)**：① 单一真相源——同一信息不双写；② 记忆层只追加不回改，推翻只改状态字段；③ AI 不擅自落盘、不编造编号、不替 PM 拍板。

</aside>

---

## 二、顶层架构：三段式

| 段       | 目录           | 职责            | 唯一性    |
| ------- | ------------ | ------------- | ------ |
| **共享层** | `_共享/`       | 宪章、AI 契约、通用知识 | 全局唯一一份 |
| **模板层** | `_模板/`       | 项目骨架母版，克隆用    | 只读母版   |
| **项目层** | `项目/PROJ-X/` | 每个项目一份独立实例    | 逐项目隔离  |

---

## 三、完整目录结构

```
pm/
├── README.md                    ← 导航 + 心智模型(瘦身版)
│
├── _共享/                       ← 全局唯一·跨项目
│   ├── PM工作宪章.md
│   ├── AGENTS.md                ← AI 协作契约 + 真相源路由表
│   ├── 写入协议.md              ← 格式/字段/编号/分片规范
│   └── 知识库/
│       └── 全局知识库.md        ← 通用经验(GKB-)
│
├── _模板/                       ← 项目骨架母版(占位模板)
│   ├── 现状.md                  ← 驾驶舱(派生)
│   ├── INDEX.md                 ← 派生索引(按需生成·非常驻)
│   ├── 记忆/                    ← 不可变·只追加·可月度分片
│   │   ├── 进度日志.md  决策记录.md  沟通记录.md  知识库.md
│   ├── 项目管理/                ← 可变真相源(基线 + 控制面)
│   │   ├── 项目章程.md          ← 【基线】含成功指标/北极星
│   │   ├── 需求登记册.md        ← 【基线】REQ- 主线 + 范围 in/out
│   │   ├── 路线图.md            ← 【控制面】里程碑
│   │   ├── 风险登记册.md        ← 【控制面】RSK-
│   │   ├── 依赖登记册.md        ← 【控制面】DEP-
│   │   └── 干系人矩阵.md        ← 【控制面】
│   ├── 文档库/
│   │   ├── 01-需求/ 02-调研/ 03-方案/ 04-评审/ 05-验收/  ← 阶段产出·按 REQ- 命名
│   │   ├── 06-会议/ 07-报告/                              ← 按日期命名
│   │   └── (01/03/07 各含 _模板.md)
│   ├── 原型/                    ← 按 REQ-XXXX-vN/ 归类
│   └── 归档/                    ← 项目内过程收口(月度滚存)
│
├── 项目/
│   ├── PROJ-灯塔/               ← 克隆自 _模板，完全独立
│   └── PROJ-北极星/
│
└── 已结项/                      ← 整项目下线归档(原顶层“归档”改名，消歧义)
    └── PROJ-旧项目/
```

---

## 四、各层职责

- **`_共享/`**：一切“本该全局统一”的东西。改一次全局生效，杜绝多份漂移。
- **`_模板/`**：项目骨架的“母版”，永远是空占位模板。新项目 = 克隆一份到 `项目/`。
- **`项目/PROJ-X/`**：单个项目的完整记忆系统，自包含、可整体归档。
- **两级归档消歧**：项目内 `归档/` = 活跃项目里的**月度过程收口**；顶层 `已结项/` = **整个项目下线**后连实例搬入。二者概念不再撞车。

---

## 五、知识双层 + 晋升机制

| 层        | 位置                    | 存什么          | 编号         |
| -------- | --------------------- | ------------ | ---------- |
| **本地知识** | `项目/PROJ-X/记忆/知识库.md` | 带上下文的踩坑/临时结论 | `KB-0005`  |
| **全局知识** | `_共享/知识库/全局知识库.md`    | 去上下文、可复用的方法论 | `GKB-0012` |

**晋升流(Promotion)，而非复制：**

```
① 本地随手记   KB-0005  “灯塔项目埋点漏了X导致返工”(含上下文)
        │  人工验证为通用规律时提炼上抬
        ▼
② 全局沉淀     GKB-0012 “埋点评审须含数据校验清单”(去上下文)
        │
③ 本地留引用   KB-0005 状态: 已晋升→GKB-0012 (不复述内容)
```

<aside>  
💡

本地记“事件”，全局记“结论”，是“原料→成品”关系，不是同内容存两遍——因此不违反单一真相源。晋升由 PM 决定，AI 至多提示。

</aside>

---

## 六、统一编号 Schema

因项目已物理隔离，项目内编号无需项目前缀，统一 **前缀 + 4 位序号**：

| 类型         | 前缀     | 归属文件            | 说明         |
| ---------- | ------ | --------------- | ---------- |
| **需求(主线)** | `REQ-` | `项目管理/需求登记册.md` | 追溯脊椎(本轮新增) |
| 进度         | `PRG-` | `记忆/进度日志.md`    |            |
| 决策         | `DEC-` | `记忆/决策记录.md`    |            |
| 沟通         | `COM-` | `记忆/沟通记录.md`    |            |
| 本地知识       | `KB-`  | `记忆/知识库.md`     |            |
| 风险         | `RSK-` | `项目管理/风险登记册.md` |            |
| **依赖**     | `DEP-` | `项目管理/依赖登记册.md` | 本轮新增       |
| 全局知识       | `GKB-` | `_共享/知识库/`      | 全局唯一       |

> 变更点：原风险 3 位 → 统一 4 位；“跨类共享序列”作废，改为**各类独立递增**，规则写死进 `写入协议.md`。

> 刻意不加：`ASM-` 假设流(与风险/决策重叠) → 改为在决策模板中记“假设前提”字段。

---

## 七、需求追溯主线(REQ-)

以 `REQ-` 作为**对象型主键**，把散落各处的“记录型”条目串成一个需求的完整生命周期：

```
REQ-0007 (一个需求/特性)
   ├── 触发决策    DEC-0012
   ├── 产出方案    文档库/03-方案/REQ-0007-方案.md
   ├── 关联原型    原型/REQ-0007-v2/
   ├── 引发风险    RSK-0003
   ├── 涉及依赖    DEP-0002
   └── 验收结果    文档库/05-验收/REQ-0007.md
```

<aside>  
🎯

**价值**：PM 最常被问“这个需求到哪了/当初为什么这么定/验收没有”。REQ- 让任一需求一键回溯其决策依据、方案、原型、风险、依赖、验收——这是把扁平 Markdown 真正变成“可查询关系库”的脊椎。轻量原则：只给**真实特性**编号，琐碎任务不占 REQ-。

</aside>

---

## 八、项目管理域：基线 vs 控制面

同在 `项目管理/`（不建子目录，仅逻辑标注），按**变更频率**区分写入模式：

| 归类            | 文件                                | 写入特征        |
| ------------- | --------------------------------- | ----------- |
| **基线**(定了少变)  | 项目章程(含成功指标/北极星)、需求登记册(含范围 in/out) | 一次立好，变更须走决策 |
| **控制面**(持续更新) | 路线图、风险登记册、依赖登记册、干系人矩阵             | 覆盖式更新当前状态   |

- **成功指标**折入 `项目章程` 必填章节——“怎样算成功”定义清楚，验收才有标尺。
- **范围 in/out** 并入 `需求登记册`——在范围的即 REQ 列表，明确不做的单列“暂不做”区，作为范围蔓延的锚点。
- **依赖 ≠ 风险**：依赖是“等别人交付”(DEP-)，风险是“可能出事”(RSK-)，字段与管理节奏不同，单列。

---

## 九、统一条目模板

**需求 / 依赖(本轮新增)：**

```
### REQ-0007 — 2026-07-25
- 状态: 待评审 / 开发中 / 已验收 / 已砍
- 范围: 在范围 / 明确不做
- 关联: DEC-0012, RSK-0003, DEP-0002
- 产物: 方案 REQ-0007-方案.md · 原型 原型/REQ-0007-v2/ · 验收 05-验收/REQ-0007.md
- 描述: ……

### DEP-0002 — 2026-07-25
- 状态: 等待中 / 已就绪 / 已逾期
- 依赖方: (团队/人，引用干系人矩阵)
- 期望交付: 2026-08-10
- 阻塞: REQ-0007
```

**进度 / 决策 / 沟通：**

```
### PRG-0007 — 2026-07-25
- 状态: 进行中 / 已完成 / 已阻塞
- 关联: REQ-0007, DEC-0003, RSK-0001
- 正文: ……

### DEC-0007 — 2026-07-25
- 状态: 生效 / 已推翻→DEC-0011 / 待复审
- 复审: 2026-08-25
- 假设前提: (本决策成立的前提，前提失效则触发复审)
- 背景 / 决策 / 依据 / 影响: ……
- 关联: REQ-0007, RSK-0001, COM-0012

### COM-0012 — 2026-07-25
- 对象: (干系人，引用干系人矩阵)
- 关联: REQ-0007, DEC-0007
- 结论/待办: ……
```

**本地知识 / 全局知识：**

```
### KB-0005 — 2026-07-25
- 类型: 踩坑 / 经验 / 方法
- 状态: 本地 / 已晋升→GKB-0012 / 过时
- 正文: (含项目上下文)

### GKB-0012 — 2026-07-25
- 领域: 需求 / 评审 / 交付 …
- 来源: PROJ-灯塔/KB-0005
- 正文: (去上下文、可复用)
```

---

## 十、真相源路由表(更新版·写入 [AGENTS.md](http://AGENTS.md))

| 信息类型     | 唯一归属               | 他处引用   |
| -------- | ------------------ | ------ |
| 需求(主线)   | `项目管理/需求登记册.md`    | `REQ-` |
| 里程碑状态    | `项目管理/路线图.md`      | —      |
| 风险       | `项目管理/风险登记册.md`    | `RSK-` |
| 依赖       | `项目管理/依赖登记册.md`    | `DEP-` |
| 干系人      | `项目管理/干系人矩阵.md`    | 姓名/角色  |
| 成功指标/北极星 | `项目管理/项目章程.md`     | —      |
| 决策       | `记忆/决策记录.md`       | `DEC-` |
| 沟通       | `记忆/沟通记录.md`       | `COM-` |
| 进度       | `记忆/进度日志.md`       | `PRG-` |
| 本地知识     | `记忆/知识库.md`        | `KB-`  |
| 通用知识     | `_共享/知识库/全局知识库.md` | `GKB-` |
| 角色边界     | `_共享/PM工作宪章.md`    | —      |
| AI 契约    | `_共享/AGENTS.md`    | —      |
| 周报/报告    | `文档库/07-报告`        | 按日期    |

---

## 十一、域边界与写入规则

- **沟通记录(COM-)边界收紧·反空壳**：`记忆/沟通记录` 专记**干系人对齐结论、表态/立场、非正式承诺与预期**；严格排除——正式会议→`文档库/06-会议`(COM 仅引用)、产生决策→`DEC-`(COM 仅作背景)、交付承诺→`DEP-`(COM 仅作来源)、干系人身份→`干系人矩阵`。**反空壳规则**：每条 COM- 必须带“结论或影响”，纯 FYI 不入账，避免沦为空引用条。
- **文档库两层 = 两个编号段，不另建文件夹**：`文档库/` 下仍是扁平的 01-07 目录，只按性质分两段——**01-05 阶段产出**(需求/调研/方案/评审/验收，一次性、按 `REQ-XXXX-*` 命名，支持迭代挂新 REQ)与 **06-07 周期记录**(会议/报告，持续累加、按日期命名)。原 `05-会议/06-验收` 对调为 `05-验收/06-会议`，使两段编号连续、语义清晰。
- **记忆月度分片(可选·防臃肿)**：**默认单文件即可**，不预先分片；仅当某日志过大影响阅读时才按月起新文件(如 `进度日志-2026-07.md`)，旧月滚入项目内 `归档/`。不为暂不存在的规模问题提前增加复杂度。
- **原型追溯与版本**：目录按 `原型/REQ-XXXX-vN/` 归类，并在 `路线图`/`现状` 引用，保证每个 demo 都对应到需求。

**需求三件套边界(登记册 ↔ 01-需求 ↔ 03-方案)** —— 三处靠 `REQ-` 串联，互不复述内容：

| 内容                            | 位置                         | 性质        |
| ----------------------------- | -------------------------- | --------- |
| 需求总账/清单(编号·标题·状态·范围·关联)       | `项目管理/需求登记册.md`            | 索引·控制面真相源 |
| 单个需求详情(PRD·用户故事·需求侧验收标准·原始输入) | `文档库/01-需求/REQ-XXXX-*.md`  | 正文·一次性产出  |
| 解决方案/技术设计                     | `文档库/03-方案/REQ-XXXX-方案.md` | 正文·一次性产出  |

- **PRD 的家**：`文档库/01-需求/REQ-XXXX-PRD.md`(回答 what/why/for whom)；若 PRD 内含技术方案，方案部分拆到 `03-方案`，避免双写。
- **需求列表的家**：`项目管理/需求登记册.md`(只存条目级总账，不存详细正文)。
- **流转**：登记册建 `REQ-0007` 一行(总账) → PRD 写进 `01-需求/REQ-0007-PRD.md`(正文) → 方案落 `03-方案/REQ-0007-方案.md`，全程只靠 `REQ-0007` 关联，任一处不复述另一处内容。

---

## 十二、[现状.md](http://xn--n4xtu.md) —— 项目驾驶舱

从“派生快照”升级为**行动入口**，顶部带 `最后更新: YYYY-MM-DD`。为控制维护成本，只列**开放项**(已关闭的不进快照)，聚合而非复制：

1. **本周焦点**(唯一手填字段)
2. **当前里程碑**(引 `路线图`，一行)
3. **当前阻塞与待办**(合并未关闭 `RSK-`/`DEP-` 与待决策 `DEC-`)
4. **到期复审提醒**(聚合各文件 `复审` 字段)

---

## 十三、INDEX 检索层(轻量·按需)

为避免与 `需求登记册` 及各记忆文件重复维护，INDEX **不作为需手工维护的常驻文件**：日常检索优先用 grep + `需求登记册`；仅当需要跨类全景视图时，由脚本/AI **按需生成**一份(列 编号/标题/日期/状态，标注“可重建·非真相源”)。

---

## 十四、迁移步骤(现结构 → v2)

1. 新建三顶层：`_共享/`、`_模板/`、`项目/`；顶层旧 `归档/` 改名为 `已结项/`。
2. `PM工作宪章.md`、`AGENTS.md` → 移入 `_共享/`；`工作区说明.md` 拆分：导航留 `README.md`，格式/字段/编号/分片规范下沉到 `_共享/写入协议.md`。
3. 现有 `记忆/项目管理/文档库/原型/归档` → 复制为 `_模板/` 母版并清空为占位模板。
4. `_模板/项目管理/` 补 `需求登记册.md`(含范围 in/out)、`依赖登记册.md`；`项目章程.md` 补“成功指标/北极星”章节。
5. 当前在跑项目 → 克隆 `_模板/` 到 `项目/PROJ-X/` 并填实。
6. 旧 `知识库.md` 中的通用条目 → 提炼上抬到 `_共享/知识库/全局知识库.md`(编号转 `GKB-`)。
7. 全库编号统一为 `前缀+4 位`(风险补零)，启用 `REQ-`/`DEP-`；决策模板补“假设前提”。
8. 文档库补 `_模板.md`；将 `05-会议/06-验收` 对调为 `05-验收/06-会议`，并对齐 01-05 阶段(REQ-)/06-07 周期(日期)命名约定。

---

## 十五、保留 / 改动 / 新增对照

| 处理    | 内容                                                                                                                       |
| ----- | ------------------------------------------------------------------------------------------------------------------------ |
| ✅ 保留  | 时间轴三时态、单一真相源、只追加、AI 最小权限、宪章角色边界                                                                                          |
| 🔧 改动 | 单命名空间→三段式；风险编号补位；跨类共享序列作废；README 瘦身拆出写入协议；顶层归档→已结项；文档库改命名约定；现状.md→驾驶舱；决策补假设前提                                            |
| ➕ 新增  | `_共享/`+`_模板/`+`项目/` 三层、知识双层+晋升机制、**REQ- 追溯主线**、**DEP- 依赖登记**、需求登记册(含范围)、章程含成功指标、基线/控制面标注、会议↔沟通边界、记忆月度分片、原型追溯版本、INDEX 索引层 |

---

## 十六、本轮增补的取舍说明(反冗余论证)

经多视角(PM 实用性 / 单一真相源 / 简单优先)复审，以下建议被**刻意否决或降级**，以防冗余与过度工程：

| 否决项                  | 理由            | 替代方案         |
| -------------------- | ------------- | ------------ |
| 独立 `成功指标.md`         | 与项目章程职责重叠     | 折入章程必填章节     |
| 独立 `范围基线.md`         | 与需求登记重叠       | 并入 `需求登记册`   |
| `项目管理/基线·控制面/` 物理子目录 | 增加嵌套，违“简单优先”  | 改为逻辑标注       |
| 文档库物理拆“阶段/周期”        | 与 REQ 命名约定重复  | 改为文件命名约定     |
| 新增 `ASM-` 假设流        | 与风险/决策重叠，编号膨胀 | 折入决策“假设前提”字段 |
